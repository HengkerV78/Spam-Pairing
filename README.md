# Install in Termux
```bash
apt update && apt upgrade -y
apt install nodejs git -y
apt install yarn -y
git clone https://github.com/HengkerV78/SpamV80.git
cd SpamV80
npm install
yarn install
npm start
